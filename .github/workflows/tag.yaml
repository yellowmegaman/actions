name: release-deployment
on:
  push:
jobs:
  gcs-release:
    name: release
    runs-on: ubuntu-latest
    env:
      REPOS: a b c d
    steps:
      - name: checkout code
        uses: actions/checkout@master
      - name: checkout gitops
        uses: actions/checkout@master
        with:
          repository: worldr/gitops
          path: gitops
          token: "${{secrets.TKN}}"
      - name: pr-donwstream
        uses: yellowmegaman/pr-downstream@master
        with:
          token: ${{secrets.TKN}}
          change: "echo $(date) | tee check/date.txt"
          title: "dummy"
          body: "dummy"
